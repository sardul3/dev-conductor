# Cursor port

`dev-loop/install.sh` copies skills from `../skills/` into `~/.cursor/skills` and `~/.agents/skills`, and installs `dev-loop/rules/dev-loop.mdc`.

```bash
./cursor/dev-loop/install.sh
```
