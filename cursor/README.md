# Cursor port of /dev-loop

Copies of loop skills and `rules/dev-loop.mdc`. Source of truth is `../skills/` and `../claude/`.

```bash
./cursor/dev-loop/install.sh
```

Cursor **product** settings, extensions, and path-scoped stack rules live in `~/dev/mac-ai-setup/cursor/`.
