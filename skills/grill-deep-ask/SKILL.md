---
name: grill-deep-ask
description: Alias for design-tree-interview so older prompt-enrich hooks still resolve. Use when the enrich pipeline names grill-deep-ask. If the work is a /dev-loop spec, use spec mode and do not launch a clean Claude.
---

# Grill deep-ask

Follow `design-tree-interview` (same repo or `~/.claude/skills/design-tree-interview/SKILL.md`).

This name exists so older hook text still resolves. Same three endings as that skill: spec.md vs Cursor handoff vs Claude `launch-clean-claude.sh`.
